// ********RoostGPT********
/*
Test generated by RoostGPT for test homeloan-AT using AI Type Open AI and AI Model gpt-4o

Test generated for /api/loan-applications/{applicationId}/documents/sign_put for http method type PUT in rest-assured framework

RoostTestHash=e4c45b62a5

[
  {
    "ScenarioID": "1",
    "TestName": "testValidRequestReturns200",
    "Description": "Test a valid request with proper path and authorization",
    "Execution": {
      "Arrange": "Prepare a valid 'applicationId' and a valid Bearer token",
      "Act": "Send PUT request to /api/loan-applications/{applicationId}/documents/sign with correct headers",
      "Assert": "Check for 200 status and response body contains confirmationMessage, signedDocumentIds, processingTimestamp, and nextSteps"
    },
    "Validation": "Ensures API successfully returns correct response with valid input"
  },
  {
    "ScenarioID": "2",
    "TestName": "testMissingApplicationIdReturns404",
    "Description": "Test missing applicationId in the path",
    "Execution": {
      "Arrange": "Prepare a valid Bearer token without applicationId",
      "Act": "Send PUT request to /api/loan-applications//documents/sign with correct headers",
      "Assert": "Check for 404 status and error message indicating documents not found"
    },
    "Validation": "Ensures API returns 404 when applicationId is missing"
  },
  {
    "ScenarioID": "3",
    "TestName": "testInvalidApplicationIdReturns404",
    "Description": "Test invalid applicationId format",
    "Execution": {
      "Arrange": "Prepare an invalid 'applicationId' (e.g., special characters) and a valid Bearer token",
      "Act": "Send PUT request to /api/loan-applications/{invalidId}/documents/sign with correct headers",
      "Assert": "Check for 404 status and error message indicating documents not found"
    },
    "Validation": "Ensures API returns 404 for invalid applicationId format"
  },
  {
    "ScenarioID": "4",
    "TestName": "testMissingAuthTokenReturns401",
    "Description": "Test request without authorization token",
    "Execution": {
      "Arrange": "Prepare a valid 'applicationId' without Bearer token",
      "Act": "Send PUT request to /api/loan-applications/{applicationId}/documents/sign without authorization header",
      "Assert": "Check for 401 status indicating unauthorized access"
    },
    "Validation": "Ensures API requires authorization token"
  },
  {
    "ScenarioID": "5",
    "TestName": "testInvalidAuthTokenReturns401",
    "Description": "Test request with an invalid authorization token",
    "Execution": {
      "Arrange": "Prepare a valid 'applicationId' and an invalid Bearer token",
      "Act": "Send PUT request to /api/loan-applications/{applicationId}/documents/sign with invalid authorization header",
      "Assert": "Check for 401 status indicating unauthorized access"
    },
    "Validation": "Ensures API rejects invalid authorization tokens"
  },
  {
    "ScenarioID": "6",
    "TestName": "testEmptySignedDocumentIdsReturns200",
    "Description": "Test response with empty signedDocumentIds array",
    "Execution": {
      "Arrange": "Prepare a valid 'applicationId' and a valid Bearer token",
      "Act": "Send PUT request to /api/loan-applications/{applicationId}/documents/sign with correct headers",
      "Assert": "Check for 200 status and response body contains an empty signedDocumentIds array"
    },
    "Validation": "Ensures API can handle cases where no documents are signed"
  },
  {
    "ScenarioID": "7",
    "TestName": "testMalformedPathReturns404",
    "Description": "Test request with malformed path",
    "Execution": {
      "Arrange": "Prepare a valid Bearer token",
      "Act": "Send PUT request to /api/loan-applications/{applicationId}/documents/sig with correct headers",
      "Assert": "Check for 404 status indicating endpoint not found"
    },
    "Validation": "Ensures API returns 404 for malformed endpoint paths"
  },
  {
    "ScenarioID": "8",
    "TestName": "testValidRequestWithEmptyBodyReturns200",
    "Description": "Test valid request with empty body",
    "Execution": {
      "Arrange": "Prepare a valid 'applicationId' and a valid Bearer token",
      "Act": "Send PUT request to /api/loan-applications/{applicationId}/documents/sign with correct headers and empty body",
      "Assert": "Check for 200 status and response body contains confirmationMessage, signedDocumentIds, processingTimestamp, and nextSteps"
    },
    "Validation": "Ensures API handles empty body correctly when not required"
  }
]

*/

// ********RoostGPT********
package com.medeiros.RoostTest;
applicationId,baseUrl
import org.json.JSONObject;
import org.json.JSONArray;
import io.restassured.RestAssured;
import io.restassured.response.Response;
import io.restassured.path.json.JsonPath;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import java.util.*;
import static io.restassured.RestAssured.given;
import static org.hamcrest.Matchers.*;
import org.hamcrest.MatcherAssert;

public class ApiLoanapplicationsApplicationIdDocumentsSignPutTest {

    List<JSONObject> testCases = new ArrayList<>();

    @BeforeEach
    public void setUp() {
        TestdataLoader dataloader = new TestdataLoader();
        testCases = dataloader.loadJson("src/test/java/com/medeiros/RoostTest/api_loan-applications_applicationId_documents_signPutTest.JSON");
    }


    @Test
    public void ApiLoanapplicationsApplicationIdDocumentsSignPut_Test() {
        this.setUp();
        Integer testNumber = 1;
        for (JSONObject testData : testCases) {
            RestAssured.baseURI = "{baseUrl}";

            JSONObject requestHeader = testData.optJSONObject("request_header");
            JSONObject requestBody = testData.optJSONObject("request_body");
            JSONObject queryParams = testData.optJSONObject("query_params");
            JSONObject pathParams = testData.optJSONObject("path_params");
            String requestPath = "/api/loan-applications/{applicationId}/documents/sign";

            if (pathParams != null) {
                for (String key : pathParams.keySet()) {
                    requestPath = requestPath.replace("{" + key + "}", pathParams.getString(key));
                }
            }

            io.restassured.specification.RequestSpecification req = given();

            // Resolve Authorization token priority: ENV > Param(RoostENV) > JSON
            String userEnvAuthToken = System.getenv("AUTH_TOKEN");
            String resolvedAuthToken = (userEnvAuthToken != null && !userEnvAuthToken.isEmpty()) ? userEnvAuthToken : "/var/tmp/Roost/RoostGPT/homeloan-AT/b0fc1826-0d80-4c1c-84b2-8595fd39aa80/20251027-073651-roost-vscode-api-musicblender-java-test.yaml";

            boolean authSet = false;
            if (requestHeader != null) {
                Iterator<String> keys = requestHeader.keys();
                while (keys.hasNext()) {
                    String key = keys.next();
                    String value = requestHeader.getString(key);

                    if (key.equalsIgnoreCase("Authorization")) {
                        if (value.equalsIgnoreCase("${AUTH_TOKEN}") && resolvedAuthToken != null && !resolvedAuthToken.isEmpty()) {
                            req.header("Authorization", "Bearer " + resolvedAuthToken);
                            authSet = true;
                        } else {
                            req.header(key, value);
                            authSet = true;
                        }
                    } else {
                        req.header(key, value);
                    }
                }
            }

            if (!authSet && resolvedAuthToken != null && !resolvedAuthToken.isEmpty()) {
                req.header("Authorization", "Bearer " + resolvedAuthToken);
            }

            if (queryParams != null) {
                for (String key : queryParams.keySet()) {
                    req.queryParam(key, queryParams.get(key));
                }
            }

            if (requestBody != null && requestBody.length() > 0) {
                req.body(requestBody.toString());
            }

            Response responseObj;
            switch ("put".toLowerCase()) {
              case "post":
                responseObj = req.when().post(requestPath);
                break;
              case "get":
                responseObj = req.when().get(requestPath);
                break;
              case "put":
                responseObj = req.when().put(requestPath);
                break;
              case "delete":
                responseObj = req.when().delete(requestPath);
                break;
              default:
                throw new RuntimeException("Unsupported HTTP method: " + "put");
            }

            responseObj = responseObj.then().extract().response();

            System.out.printf("Test Case %d: ApiLoanapplicationsApplicationIdDocumentsSignPut_Test\n", testNumber++);
            System.out.println("Request: PUT " + requestPath);
            System.out.println("Status Code: " + responseObj.statusCode());

            if (testData.has("expected_status_code")) {
                int expectedCode = testData.getInt("expected_status_code");
                MatcherAssert.assertThat(
                    "Expected status code " + expectedCode + " but got " + responseObj.statusCode(),
                    responseObj.statusCode(),
                    equalTo(expectedCode)
                );
            }
        }
    }
}
