// ********RoostGPT********
/*
Test generated by RoostGPT for test homeloan-AT using AI Type Open AI and AI Model gpt-4o

Test generated for /api/system/browser-compatibility_get for http method type GET in rest-assured framework

RoostTestHash=2c4584a354

[
  {
    "ScenarioID": "1",
    "TestName": "testValidRequestReturns200",
    "Description": "Test a valid request to retrieve browser compatibility information",
    "Execution": {
      "Arrange": "No specific arrangement needed as there are no parameters",
      "Act": "Send GET request to /api/system/browser-compatibility",
      "Assert": "Check for 200 status and response body contains 'supportedBrowsers' array"
    },
    "Validation": "Ensures API successfully returns correct response with valid input"
  },
  {
    "ScenarioID": "2",
    "TestName": "testResponseContainsBrowserInformation",
    "Description": "Ensure the response contains browser information with expected properties",
    "Execution": {
      "Arrange": "No specific arrangement needed as there are no parameters",
      "Act": "Send GET request to /api/system/browser-compatibility",
      "Assert": "Verify each item in 'supportedBrowsers' contains 'browser', 'minimum_version', and 'recommended_version'"
    },
    "Validation": "Ensures each browser entry contains necessary fields"
  },
  {
    "ScenarioID": "3",
    "TestName": "testEmptyResponseBody",
    "Description": "Test the scenario where the response body is empty",
    "Execution": {
      "Arrange": "No specific arrangement needed as there are no parameters",
      "Act": "Send GET request to /api/system/browser-compatibility",
      "Assert": "Check that the response body is not empty and contains 'supportedBrowsers'"
    },
    "Validation": "Ensures the response body is not empty and contains expected data"
  },
  {
    "ScenarioID": "4",
    "TestName": "testResponseWithEmptySupportedBrowsers",
    "Description": "Test the scenario where 'supportedBrowsers' array is empty",
    "Execution": {
      "Arrange": "No specific arrangement needed as there are no parameters",
      "Act": "Send GET request to /api/system/browser-compatibility",
      "Assert": "Check that 'supportedBrowsers' is an array and can be empty"
    },
    "Validation": "Ensures the API can handle cases where no browsers are supported"
  },
  {
    "ScenarioID": "5",
    "TestName": "testInvalidHTTPMethod",
    "Description": "Test using an invalid HTTP method to access the endpoint",
    "Execution": {
      "Arrange": "No specific arrangement needed as there are no parameters",
      "Act": "Send POST request to /api/system/browser-compatibility",
      "Assert": "Check for 405 Method Not Allowed status"
    },
    "Validation": "Ensures the API only allows GET requests"
  },
  {
    "ScenarioID": "6",
    "TestName": "testMalformedURL",
    "Description": "Test accessing the endpoint with a malformed URL",
    "Execution": {
      "Arrange": "No specific arrangement needed as there are no parameters",
      "Act": "Send GET request to /api/system/browser-compatibilty (misspelled)",
      "Assert": "Check for 404 Not Found status"
    },
    "Validation": "Ensures the API returns correct status for malformed URLs"
  }
]

*/

// ********RoostGPT********
package com.medeiros.RoostTest;
baseUrl
import org.json.JSONObject;
import org.json.JSONArray;
import io.restassured.RestAssured;
import io.restassured.response.Response;
import io.restassured.path.json.JsonPath;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import java.util.*;
import static io.restassured.RestAssured.given;
import static org.hamcrest.Matchers.*;
import org.hamcrest.MatcherAssert;

public class ApiSystemBrowsercompatibilityGetTest {

    List<JSONObject> testCases = new ArrayList<>();

    @BeforeEach
    public void setUp() {
        TestdataLoader dataloader = new TestdataLoader();
        testCases = dataloader.loadJson("src/test/java/com/medeiros/RoostTest/api_system_browser-compatibilityGetTest.JSON");
    }


    @Test
    public void ApiSystemBrowsercompatibilityGet_Test() {
        this.setUp();
        Integer testNumber = 1;
        for (JSONObject testData : testCases) {
            RestAssured.baseURI = "{baseUrl}";

            JSONObject requestHeader = testData.optJSONObject("request_header");
            JSONObject requestBody = testData.optJSONObject("request_body");
            JSONObject queryParams = testData.optJSONObject("query_params");
            JSONObject pathParams = testData.optJSONObject("path_params");
            String requestPath = "/api/system/browser-compatibility";

            if (pathParams != null) {
                for (String key : pathParams.keySet()) {
                    requestPath = requestPath.replace("{" + key + "}", pathParams.getString(key));
                }
            }

            io.restassured.specification.RequestSpecification req = given();

            // Resolve Authorization token priority: ENV > Param(RoostENV) > JSON
            String userEnvAuthToken = System.getenv("AUTH_TOKEN");
            String resolvedAuthToken = (userEnvAuthToken != null && !userEnvAuthToken.isEmpty()) ? userEnvAuthToken : "/var/tmp/Roost/RoostGPT/homeloan-AT/b0fc1826-0d80-4c1c-84b2-8595fd39aa80/20251027-073651-roost-vscode-api-musicblender-java-test.yaml";

            boolean authSet = false;
            if (requestHeader != null) {
                Iterator<String> keys = requestHeader.keys();
                while (keys.hasNext()) {
                    String key = keys.next();
                    String value = requestHeader.getString(key);

                    if (key.equalsIgnoreCase("Authorization")) {
                        if (value.equalsIgnoreCase("${AUTH_TOKEN}") && resolvedAuthToken != null && !resolvedAuthToken.isEmpty()) {
                            req.header("Authorization", "Bearer " + resolvedAuthToken);
                            authSet = true;
                        } else {
                            req.header(key, value);
                            authSet = true;
                        }
                    } else {
                        req.header(key, value);
                    }
                }
            }

            if (!authSet && resolvedAuthToken != null && !resolvedAuthToken.isEmpty()) {
                req.header("Authorization", "Bearer " + resolvedAuthToken);
            }

            if (queryParams != null) {
                for (String key : queryParams.keySet()) {
                    req.queryParam(key, queryParams.get(key));
                }
            }

            if (requestBody != null && requestBody.length() > 0) {
                req.body(requestBody.toString());
            }

            Response responseObj;
            switch ("get".toLowerCase()) {
              case "post":
                responseObj = req.when().post(requestPath);
                break;
              case "get":
                responseObj = req.when().get(requestPath);
                break;
              case "put":
                responseObj = req.when().put(requestPath);
                break;
              case "delete":
                responseObj = req.when().delete(requestPath);
                break;
              default:
                throw new RuntimeException("Unsupported HTTP method: " + "get");
            }

            responseObj = responseObj.then().extract().response();

            System.out.printf("Test Case %d: ApiSystemBrowsercompatibilityGet_Test\n", testNumber++);
            System.out.println("Request: GET " + requestPath);
            System.out.println("Status Code: " + responseObj.statusCode());

            if (testData.has("expected_status_code")) {
                int expectedCode = testData.getInt("expected_status_code");
                MatcherAssert.assertThat(
                    "Expected status code " + expectedCode + " but got " + responseObj.statusCode(),
                    responseObj.statusCode(),
                    equalTo(expectedCode)
                );
            }
        }
    }
}
