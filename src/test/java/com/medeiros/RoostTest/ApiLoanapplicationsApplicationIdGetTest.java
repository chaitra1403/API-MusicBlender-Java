// ********RoostGPT********
/*
Test generated by RoostGPT for test homeloan-AT using AI Type Open AI and AI Model gpt-4o

Test generated for /api/loan-applications/{applicationId}_get for http method type GET in rest-assured framework

RoostTestHash=88bd388e0f

[
  {
    "ScenarioID": "1",
    "TestName": "testValidRequestReturns200",
    "Description": "Test a valid request with a proper applicationId path param and valid auth token",
    "Execution": {
      "Arrange": "Prepare a valid applicationId and a valid Bearer token",
      "Act": "Send GET request to /api/loan-applications/{applicationId} with correct headers",
      "Assert": "Check for 200 status and response body contains application details"
    },
    "Validation": "Ensures API successfully returns correct response with valid input"
  },
  {
    "ScenarioID": "2",
    "TestName": "testMissingApplicationIdReturns404",
    "Description": "Test request with missing applicationId path param",
    "Execution": {
      "Arrange": "Prepare a valid Bearer token",
      "Act": "Send GET request to /api/loan-applications/ without applicationId",
      "Assert": "Check for 404 status indicating application not found"
    },
    "Validation": "Ensures API returns 404 when applicationId is missing"
  },
  {
    "ScenarioID": "3",
    "TestName": "testInvalidApplicationIdFormatReturns404",
    "Description": "Test request with invalid applicationId format",
    "Execution": {
      "Arrange": "Prepare an invalid applicationId (e.g., special characters) and a valid Bearer token",
      "Act": "Send GET request to /api/loan-applications/{invalidApplicationId}",
      "Assert": "Check for 404 status indicating application not found"
    },
    "Validation": "Ensures API handles invalid applicationId format correctly"
  },
  {
    "ScenarioID": "4",
    "TestName": "testValidRequestWithMissingTokenReturns401",
    "Description": "Test a valid request with missing Bearer token",
    "Execution": {
      "Arrange": "Prepare a valid applicationId",
      "Act": "Send GET request to /api/loan-applications/{applicationId} without Bearer token",
      "Assert": "Check for 401 status indicating unauthorized access"
    },
    "Validation": "Ensures API requires authentication for access"
  },
  {
    "ScenarioID": "5",
    "TestName": "testValidRequestWithInvalidTokenReturns401",
    "Description": "Test a valid request with an invalid Bearer token",
    "Execution": {
      "Arrange": "Prepare a valid applicationId and an invalid Bearer token",
      "Act": "Send GET request to /api/loan-applications/{applicationId} with invalid token",
      "Assert": "Check for 401 status indicating unauthorized access"
    },
    "Validation": "Ensures API rejects requests with invalid authentication tokens"
  },
  {
    "ScenarioID": "6",
    "TestName": "testNonExistentApplicationIdReturns404",
    "Description": "Test request with a non-existent applicationId",
    "Execution": {
      "Arrange": "Prepare a non-existent applicationId and a valid Bearer token",
      "Act": "Send GET request to /api/loan-applications/{nonExistentApplicationId}",
      "Assert": "Check for 404 status indicating application not found"
    },
    "Validation": "Ensures API returns 404 for non-existent applicationId"
  },
  {
    "ScenarioID": "7",
    "TestName": "testValidRequestReturnsCorrectSchema",
    "Description": "Test a valid request to ensure response schema is correct",
    "Execution": {
      "Arrange": "Prepare a valid applicationId and a valid Bearer token",
      "Act": "Send GET request to /api/loan-applications/{applicationId}",
      "Assert": "Check response body matches expected schema with all required fields"
    },
    "Validation": "Ensures API response adheres to the defined schema"
  }
]

*/

// ********RoostGPT********
package com.medeiros.RoostTest;
applicationId,baseUrl
import org.json.JSONObject;
import org.json.JSONArray;
import io.restassured.RestAssured;
import io.restassured.response.Response;
import io.restassured.path.json.JsonPath;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import java.util.*;
import static io.restassured.RestAssured.given;
import static org.hamcrest.Matchers.*;
import org.hamcrest.MatcherAssert;

public class ApiLoanapplicationsApplicationIdGetTest {

    List<JSONObject> testCases = new ArrayList<>();

    @BeforeEach
    public void setUp() {
        TestdataLoader dataloader = new TestdataLoader();
        testCases = dataloader.loadJson("src/test/java/com/medeiros/RoostTest/api_loan-applications_applicationIdGetTest.JSON");
    }


    @Test
    public void ApiLoanapplicationsApplicationIdGet_Test() {
        this.setUp();
        Integer testNumber = 1;
        for (JSONObject testData : testCases) {
            RestAssured.baseURI = "{baseUrl}";

            JSONObject requestHeader = testData.optJSONObject("request_header");
            JSONObject requestBody = testData.optJSONObject("request_body");
            JSONObject queryParams = testData.optJSONObject("query_params");
            JSONObject pathParams = testData.optJSONObject("path_params");
            String requestPath = "/api/loan-applications/{applicationId}";

            if (pathParams != null) {
                for (String key : pathParams.keySet()) {
                    requestPath = requestPath.replace("{" + key + "}", pathParams.getString(key));
                }
            }

            io.restassured.specification.RequestSpecification req = given();

            // Resolve Authorization token priority: ENV > Param(RoostENV) > JSON
            String userEnvAuthToken = System.getenv("AUTH_TOKEN");
            String resolvedAuthToken = (userEnvAuthToken != null && !userEnvAuthToken.isEmpty()) ? userEnvAuthToken : "/var/tmp/Roost/RoostGPT/homeloan-AT/b0fc1826-0d80-4c1c-84b2-8595fd39aa80/20251027-073651-roost-vscode-api-musicblender-java-test.yaml";

            boolean authSet = false;
            if (requestHeader != null) {
                Iterator<String> keys = requestHeader.keys();
                while (keys.hasNext()) {
                    String key = keys.next();
                    String value = requestHeader.getString(key);

                    if (key.equalsIgnoreCase("Authorization")) {
                        if (value.equalsIgnoreCase("${AUTH_TOKEN}") && resolvedAuthToken != null && !resolvedAuthToken.isEmpty()) {
                            req.header("Authorization", "Bearer " + resolvedAuthToken);
                            authSet = true;
                        } else {
                            req.header(key, value);
                            authSet = true;
                        }
                    } else {
                        req.header(key, value);
                    }
                }
            }

            if (!authSet && resolvedAuthToken != null && !resolvedAuthToken.isEmpty()) {
                req.header("Authorization", "Bearer " + resolvedAuthToken);
            }

            if (queryParams != null) {
                for (String key : queryParams.keySet()) {
                    req.queryParam(key, queryParams.get(key));
                }
            }

            if (requestBody != null && requestBody.length() > 0) {
                req.body(requestBody.toString());
            }

            Response responseObj;
            switch ("get".toLowerCase()) {
              case "post":
                responseObj = req.when().post(requestPath);
                break;
              case "get":
                responseObj = req.when().get(requestPath);
                break;
              case "put":
                responseObj = req.when().put(requestPath);
                break;
              case "delete":
                responseObj = req.when().delete(requestPath);
                break;
              default:
                throw new RuntimeException("Unsupported HTTP method: " + "get");
            }

            responseObj = responseObj.then().extract().response();

            System.out.printf("Test Case %d: ApiLoanapplicationsApplicationIdGet_Test\n", testNumber++);
            System.out.println("Request: GET " + requestPath);
            System.out.println("Status Code: " + responseObj.statusCode());

            if (testData.has("expected_status_code")) {
                int expectedCode = testData.getInt("expected_status_code");
                MatcherAssert.assertThat(
                    "Expected status code " + expectedCode + " but got " + responseObj.statusCode(),
                    responseObj.statusCode(),
                    equalTo(expectedCode)
                );
            }
        }
    }
}
